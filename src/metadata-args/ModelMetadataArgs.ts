/**
 * Metadata used to store registered models.
 */
export interface ModelMetadataArgs {
    
    /**
     * Target class.
     */
    target: Function;
    
    /**
     * Model name.
     */
    name: string;

}