/**
 * Metadata used to store registered request maps.
 */
export interface RequestMapMetadataArgs {
    
    /**
     * Target class.
     */
    target: Function;
    
    /**
     * Model name.
     */
    name: string;

}